# ETS2 TruckersMP Chat Translator v2.2.0

## 更新内容

### 代码质量修复（50+ 缺陷）
- **数据安全**：DPAPI 解密失败不再覆盖原始密钥；配置损坏自动备份；原子保存防止热重载竞争
- **线程安全**：广告发送不再跨线程访问 Tkinter；关闭时正确 join 线程；热键解析支持特殊按键
- **剪贴板安全**：发送消息后始终恢复原始剪贴板；先分配内存再清空剪贴板；emoji 缓冲区正确计算 UTF-16 长度
- **日志系统**：Windows 日志轮转修复；写入失败不再静默吞掉
- **窗口显示**：毛玻璃失败回退为不透明窗口（不再产生拖影）；字体大小更新覆盖基准文本
- **翻译引擎**：混合语言空格保留；批量回退不再被永久缓存；_should_skip 支持所有目标语言

### 移除百度翻译
- 删除 `translate_via_baidu`、`test_baidu_connection` 等全部百度 API 代码
- 删除百度千帆、百度翻译两个 Provider 预设
- 删除配置中的百度字段和迁移逻辑
- 清理百度相关 UI 和测试

### 翻译提示简化
- 接收翻译：从 133 行 system prompt 简化为单条 user 消息
- 与发送翻译保持一致的简洁风格

### 其他
- 修复 2 个运行时 bug（设置窗口关闭报错、Provider 按钮点击报错）
- Provider 编辑时手动修改端点自动断开预设关联
- 版本号更新至 v2.2.0
