# v2.3.1 — 非文字跳过 + 多模型竞速

## 🛡️ 非文字内容跳过翻译

修复大模型把标点符号、数字、emoji 等非文字字符"翻译"成无关文字的问题：

- 新增非文字检测：纯标点（`.`、`...`）、纯数字（`123`）、纯 emoji、空消息 → **直接跳过，不再发送给大模型**
- 例如之前 `.` 会被翻译成 `TruckersMP/ETS2 多人游戏聊天记录：...`，现在直接原样显示

## ⚡ 翻译模式改为多模型并行竞速

- 从「轮转负载均衡」改为「**并行竞速**」：一条消息**同时发给所有已启用的 Provider**，谁先返回有效译文就用谁
- 大幅降低单条消息的翻译延迟（不再等轮到某个 Provider）
- 保留熔断冷却 + 未翻译校验：返回原文/非目标语言的 Provider 结果自动忽略，换下一个有效结果

## 📦 其他

- 版本号更新至 v2.3.1
