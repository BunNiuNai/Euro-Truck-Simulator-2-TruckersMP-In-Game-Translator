# ETS2 翻译器 v1.6.0 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 5 项功能改进：Provider 顺序模式、翻译提示词修正、发送目标语言选择、消息语言标签、语言选项中文全称统一。

**Architecture:** 在现有消息管线 (monitor → translator → overlay) 上增量修改。config.py 新增 3 个配置字段，translator.py 新增顺序调用和语言检测逻辑，overlay.py 修正提示文案并渲染语言标签，main.py 更新设置面板 UI。

**Tech Stack:** Python 3.10+, Tkinter, httpx, ctypes (Win32 API)

---

## 改动 5: 语言选项统一中文全称 (先做，后续改动依赖)

### Task 1: 更新 `_lang_map` 为中文全称

**Files:**
- Modify: `main.py:L619-L624`

- [ ] **Step 1: 修改语言映射表**

将 `main.py` SettingsDialog 中的 `_lang_map` 全部改为中文全称：

```python
# L619-L624, 替换为:
self._lang_map: dict[str, str] = {
    "简体中文": "zh-CN", "英语": "en", "日语": "ja", "韩语": "ko",
    "法语": "fr", "德语": "de", "西班牙语": "es",
    "俄语": "ru", "葡萄牙语": "pt", "意大利语": "it",
}
```

- [ ] **Step 2: 运行现有测试确认无回归**

```bash
cd "y:/翻译器项目/ets2-translator" && python -m pytest test_provider_config.py test_compose_sender.py test_logger.py -v
```

预期: 全部 PASS

- [ ] **Step 3: 提交**

```bash
git add main.py
git commit -m "refactor: 语言选项统一为中文全称显示"
```

---

## 改动 3: 发送目标语言选择

### Task 2: 新增 `send_target_language` 配置字段

**Files:**
- Modify: `config.py:L198-L226` (AppConfig dataclass)

- [ ] **Step 1: 在 AppConfig 中新增字段**

```python
# 在 AppConfig dataclass 中新增 (L225 附近，debug_log 之后):
    send_target_language: str = "en"  # target language for sending translation
```

- [ ] **Step 2: 验证配置可正常加载**

```bash
cd "y:/翻译器项目/ets2-translator" && python -c "from config import AppConfig, load_config; c = load_config(); print(f'send_target_language={c.send_target_language}')"
```

预期输出: `send_target_language=en`

- [ ] **Step 3: 提交**

```bash
git add config.py
git commit -m "feat: 新增 send_target_language 配置字段"
```

### Task 3: 参数化发送翻译函数

**Files:**
- Modify: `translator.py:L596-L681` (translate_to_english 函数及相关)

- [ ] **Step 1: 重命名并参数化 translate_to_english**

将 `translate_to_english` 重命名为 `translate_for_send`，增加 `target_lang` 参数:

```python
# L599: 重命名函数
def translate_for_send(cfg: AppConfig, text: str, target_lang: str = "en") -> str:
    """Translate Chinese text to target language for sending in chat.
    Returns the translated text, or raises an exception on error."""
    # 获取目标语言的中文名用于 prompt
    lang_name_map = {
        "zh-CN": "简体中文", "en": "英语", "ja": "日语", "ko": "韩语",
        "fr": "法语", "de": "德语", "es": "西班牙语",
        "ru": "俄语", "pt": "葡萄牙语", "it": "意大利语",
    }
    target_name = lang_name_map.get(target_lang, "英语")
```

LLM prompt 中的 system message 动态构建:
```python
# L617 附近，替换 SEND_SYSTEM_PROMPT:
                "messages": [
                    {"role": "system", "content": SEND_SYSTEM_PROMPT.replace("{target_language}", target_name)},
                    {"role": "user", "content": text},
                ],
```

百度翻译分支:
```python
# L602-L603: 改为
    if cfg.translation_backend == "baidu":
        baidu_to = _lang_code_to_baidu(target_lang)
        return translate_via_baidu(cfg.baidu_appid, cfg.baidu_secret, text, to_lang=baidu_to)
```

同样处理混合模式中的百度调用和 legacy fallback。

- [ ] **Step 2: 添加百度语言代码映射辅助函数**

```python
# 在 translator.py 顶部附近，translate_to_english_via_baidu 之前:
def _lang_code_to_baidu(lang_code: str) -> str:
    """Map our internal lang codes to Baidu API lang codes."""
    _baidu_map = {
        "zh-CN": "zh", "en": "en", "ja": "jp", "ko": "kor",
        "fr": "fra", "de": "de", "es": "spa",
        "ru": "ru", "pt": "pt", "it": "it",
    }
    return _baidu_map.get(lang_code, "en")
```

- [ ] **Step 3: 更新 send_prompt.txt 使用占位符**

修改 `prompts/send_prompt.txt` 第 1 行:
```
You are a translator for ETS2/TruckersMP in-game chat. Translate Chinese into natural,
accurate English that a real gamer would type. Never summarize, never omit, never add.
```
改为:
```
You are a translator for ETS2/TruckersMP in-game chat. Translate Chinese into natural,
accurate {target_language} that a real gamer would type. Never summarize, never omit, never add.
```

- [ ] **Step 4: 更新 overlay.py 中的调用**

修改 `overlay.py:L789`:
```python
# 旧: english = translate_to_english(self.cfg, chinese_text)
# 新:
from translator import translate_for_send
english = translate_for_send(self.cfg, chinese_text, self.cfg.send_target_language)
```

- [ ] **Step 5: 删除旧函数名引用**

修改 `overlay.py:L789` 的 import:
```python
# 旧: from translator import translate_to_english
# 新: from translator import translate_for_send
```

- [ ] **Step 6: 运行测试**

```bash
cd "y:/翻译器项目/ets2-translator" && python -m pytest test_compose_sender.py -v
```

预期: 全部 PASS (ComposeSender 测试不依赖 translate_to_english)

- [ ] **Step 7: 提交**

```bash
git add translator.py overlay.py prompts/send_prompt.txt
git commit -m "feat: 发送翻译支持多种目标语言"
```

### Task 4: 设置面板新增发送语言下拉框

**Files:**
- Modify: `main.py:_build_api_tab` (L567-L671)

- [ ] **Step 1: 在 API 配置页新增发送语言选项**

在 `_build_api_tab` 中，`target_language` ComboBox 之后添加:

```python
# 在 r = self._row(card_meta, r, "Target Language / 目标语言", self.lang_combo) 之后:

self.send_lang_var = tk.StringVar(value=self._lang_rev.get(self.cfg.send_target_language, "英语"))
self.send_lang_combo = ttk.Combobox(
    card_meta, textvariable=self.send_lang_var,
    values=list(self._lang_map.keys()),
    state="readonly", width=18, font=("Microsoft YaHei", 10))
r = self._row(card_meta, r, "Send Language / 发送目标语言", self.send_lang_combo)
```

- [ ] **Step 2: 在 `_load_values` 中初始化**

```python
# 在 _load_values 方法中，L1147 之后添加:
self.send_lang_var.set(self._lang_rev.get(self.cfg.send_target_language, "英语"))
```

- [ ] **Step 3: 在 `_save` 中保存**

```python
# 在 _save 方法中，target_language 行之后添加:
send_target_language=self._lang_map.get(self.send_lang_var.get(), "en"),
```

- [ ] **Step 4: 提交**

```bash
git add main.py
git commit -m "feat: 设置面板新增发送目标语言下拉框"
```

---

## 改动 1: Provider 模式切换

### Task 5: 新增 `provider_mode` 配置字段

**Files:**
- Modify: `config.py:L198-L226`

- [ ] **Step 1: 新增字段**

```python
# 在 AppConfig 中新增:
    provider_mode: str = "race"  # "race" = parallel race, "sequential" = sequential fallback
```

- [ ] **Step 2: 验证**

```bash
cd "y:/翻译器项目/ets2-translator" && python -c "from config import AppConfig; c = AppConfig(); print(c.provider_mode)"
```

预期: `race`

- [ ] **Step 3: 提交**

```bash
git add config.py
git commit -m "feat: 新增 provider_mode 配置字段"
```

### Task 6: 实现顺序超时回退逻辑

**Files:**
- Modify: `translator.py:L414-L474` (_call_api_internal)

- [ ] **Step 1: 添加超时常量**

在 `translator.py` 顶部 (L27 附近):
```python
PROVIDER_TIMEOUT = 5.0  # seconds before giving up on a provider in sequential mode
```

- [ ] **Step 2: 修改 _call_provider 支持独立超时**

在 `_call_provider` 方法签名 (L352) 增加 timeout 参数:
```python
def _call_provider(self, provider: dict, text: str, timeout: float = 8.0) -> str:
```

然后将 `self._get_client().post(...)` 调用改用带 timeout 的临时 client:
```python
# L373: 改为使用临时 client 以支持独立超时
client = httpx.Client(timeout=timeout)
try:
    resp = client.post(endpoint, json=payload, headers=headers)
    resp.raise_for_status()
    data = resp.json()
    return data["choices"][0]["message"]["content"].strip()
finally:
    client.close()
```

- [ ] **Step 3: 在 _call_api_internal 中新增顺序分支**

在 `_call_api_internal` (L414) 开头，过滤 active providers 之后:

```python
def _call_api_internal(self, text: str) -> str:
    """Provider parallel race + serial fallback with circuit breaker."""
    providers = [p for p in self.cfg.llm_providers if p.get("enabled", True)]
    if not providers:
        return self._call_api_legacy(text)

    # Filter out cooling providers
    active = [p for p in providers if not self._is_cooling(p["label"])]
    if not active:
        log = get_logger()
        if log:
            log.warn("LLM", "所有 Provider 均处于冷却期，强制重试全部")
        active = providers

    # --- SEQUENTIAL MODE ---
    if self.cfg.provider_mode == "sequential":
        return self._call_sequential(active, providers, text)

    # --- RACE MODE (existing) ---
    # ... 保留现有并行竞速代码 ...
```

- [ ] **Step 4: 实现 _call_sequential 方法**

在 Translator 类中新增方法:

```python
def _call_sequential(self, active, all_providers, text):
    """Sequential provider fallback: try each in order, 5s timeout each."""
    log = get_logger()
    errors = []
    
    for p in active:
        label = p["label"]
        if log:
            log.info("LLM", f"顺序尝试: {label}")
        try:
            result = self._call_provider(p, text, timeout=PROVIDER_TIMEOUT)
            self._note_provider_result(label, True)
            if log:
                log.info("LLM", f"顺序成功: {label}")
            return result
        except (httpx.ConnectError, httpx.TimeoutException) as e:
            err = self._format_error(e)
            errors.append(f"{label}: {err}")
            self._note_provider_result(label, False)
            if log:
                log.warn("LLM", f"顺序失败(超时/连接): {label} - {err}")
            continue
        except httpx.HTTPStatusError as e:
            err = self._format_error(e)
            errors.append(f"{label}: {err}")
            self._note_provider_result(label, False)
            if log:
                log.warn("LLM", f"顺序失败(HTTP {e.response.status_code}): {label} - {err}")
            continue
        except Exception as e:
            err = self._format_error(e)
            errors.append(f"{label}: {err}")
            self._note_provider_result(label, False)
            if log:
                log.warn("LLM", f"顺序失败: {label} - {err}")
            continue
    
    # 全部 active 失败，串行重试全部（包括冷却中的）
    if log:
        log.warn("LLM", f"全部顺序失败，进入串行重试 ({len(all_providers)} providers)")
    for p in all_providers:
        try:
            result = self._call_provider(p, text, timeout=8.0)
            self._note_provider_result(p["label"], True)
            if log:
                log.info("LLM", f"重试成功: {p['label']}")
            return result
        except Exception:
            self._note_provider_result(p["label"], False)
            time.sleep(0.18)
    
    raise Exception(" | ".join(errors) if errors else "所有 Provider 翻译失败")
```

- [ ] **Step 5: 运行现有测试确认无回归**

```bash
cd "y:/翻译器项目/ets2-translator" && python -m pytest test_provider_config.py test_compose_sender.py test_logger.py -v
```

预期: 全部 PASS

- [ ] **Step 6: 提交**

```bash
git add translator.py
git commit -m "feat: 实现 Provider 顺序超时回退模式"
```

### Task 7: 设置面板新增 Provider 模式选项

**Files:**
- Modify: `main.py:_build_api_tab`

- [ ] **Step 1: 新增 RadioButton 组**

在 API 配置页 Provider 列表下方添加:

```python
# 在 _build_api_tab 中 provider 列表区域之后、backend 区域之前:
self._section_label(inner, "PROVIDER MODE  /  调用模式").pack(fill=tk.X, pady=(8, 8))
mode_card = self._card(inner, padx=16, pady=12)
mode_card.pack(fill=tk.X, pady=(0, 4))

self.provider_mode_var = tk.StringVar(value=self.cfg.provider_mode)
mode_frame = tk.Frame(mode_card, bg=self._CARD_BG)
for val, lbl in [("race", "Race / 竞速（并行，谁快用谁）"),
                 ("sequential", "Sequential / 顺序（逐个尝试，超时回退）")]:
    rb = tk.Radiobutton(mode_frame, text=lbl, variable=self.provider_mode_var, value=val,
                        bg=self._CARD_BG, fg=self._TEXT,
                        font=("Microsoft YaHei", 10),
                        selectcolor=self._CARD_BG,
                        activebackground=self._CARD_BG,
                        activeforeground=self._ACCENT)
    rb.pack(anchor=tk.W, pady=2)
mode_frame.pack(fill=tk.X, padx=16, pady=8)
```

- [ ] **Step 2: 在 `_save` 中保存**

```python
provider_mode=self.provider_mode_var.get(),
```

- [ ] **Step 3: 提交**

```bash
git add main.py
git commit -m "feat: 设置面板新增 Provider 模式选项"
```

---

## 改动 2: 翻译提示词按后端类型区分

### Task 8: 修正 poll_messages 中的通知文案

**Files:**
- Modify: `overlay.py:L594-L598`

- [ ] **Step 1: 修改提示文案逻辑**

```python
# L594-L598 替换为:
        if baidu_count > 0:
            self._show_notice(f"百度翻译优化了 {baidu_count} 条翻译", "#f44747")
        elif new_count > 0:
            backend = self.cfg.translation_backend
            if backend == "baidu":
                notice = f"百度翻译了 {new_count} 条消息"
            elif backend == "llm+baidu":
                notice = f"大模型+百度翻译了 {new_count} 条消息"
            else:
                notice = f"大模型翻译了 {new_count} 条消息"
            self._show_notice(notice, "#4ec9b0", "#1a2a1a")
```

- [ ] **Step 2: 提交**

```bash
git add overlay.py
git commit -m "fix: 翻译通知提示按后端类型区分显示"
```

---

## 改动 4: 消息语言标签

### Task 9: DisplayMessage 新增 detected_language 字段

**Files:**
- Modify: `message_types.py:L5-L13`

- [ ] **Step 1: 新增字段**

```python
@dataclass
class DisplayMessage:
    """A message ready for display in the overlay window."""
    player_name: str
    original_text: str
    translated_text: str
    is_self: bool = False
    baidu_fixed: bool = False
    detected_language: str = ""  # e.g. "英语", "德语", "俄语"
```

- [ ] **Step 2: 提交**

```bash
git add message_types.py
git commit -m "feat: DisplayMessage 新增 detected_language 字段"
```

### Task 10: 实现语言检测函数

**Files:**
- Modify: `translator.py` (新增函数)

- [ ] **Step 1: 实现 _detect_language 函数**

在 `translator.py` 顶部 (L23 附近，`_CJK_RE` 之后):

```python
import re as _re_module

# Unicode range detection patterns
_LANG_PATTERNS: list[tuple[str, re.Pattern]] = [
    ("日语", _re_module.compile(r"[぀-ゟ゠-ヿ]")),    # Hiragana + Katakana
    ("韩语", _re_module.compile(r"[가-힯ᄀ-ᇿ]")),    # Hangul
    ("俄语", _re_module.compile(r"[Ѐ-ӿ]")),                  # Cyrillic
    ("中文", _re_module.compile(r"[一-鿿]")),                  # CJK
    ("泰语", _re_module.compile(r"[฀-๿]")),                  # Thai
    ("阿拉伯语", _re_module.compile(r"[؀-ۿݐ-ݿ]")), # Arabic
]

# Latin-script language detection: specific characters
_LATIN_MARKERS: list[tuple[str, str]] = [
    ("德语", "ßäöüÄÖÜ"),
    ("法语", "çèéêëàâîïôùûœæÇÈÉÊËÀÂÎÏÔÙÛŒÆ"),
    ("西班牙语", "ñáéíóúü¿¡ÑÁÉÍÓÚÜ"),
    ("葡萄牙语", "ãõâêôàáéíóúçÃÕÂÊÔÀÁÉÍÓÚÇ"),
    ("意大利语", "àèéìòùÀÈÉÌÒÙ"),
]

# Common words for English detection
_ENGLISH_COMMON = {"the", "is", "are", "you", "me", "i", "he", "she", "what",
                   "where", "when", "how", "why", "can", "will", "not", "this",
                   "that", "and", "for", "have", "with", "your", "but", "all"}


def detect_language(text: str) -> str:
    """Detect the language of a given text.
    Returns Chinese name of detected language, e.g. '英语', '德语', '俄语'.
    Uses Unicode range detection for non-Latin scripts, and Latin marker
    characters for Latin-based languages.
    """
    if not text or not text.strip():
        return "未知"
    
    text_stripped = text.strip()
    
    # Priority 1: Non-Latin scripts (strong signal)
    scores: dict[str, int] = {}
    for lang_name, pattern in _LANG_PATTERNS:
        matches = len(pattern.findall(text_stripped))
        if matches > 0:
            scores[lang_name] = matches
    
    if scores:
        # Return the language with most matches
        return max(scores, key=scores.get)  # type: ignore
    
    # Priority 2: Latin-script language detection
    if _re_module.match(r"^[a-zA-Z\s\d\W]+$", text_stripped[:20]):
        # Check for language-specific characters
        for lang_name, markers in _LATIN_MARKERS:
            if any(ch in text_stripped for ch in markers):
                return lang_name
        
        # Check for common English words as fallback
        words = set(text_stripped.lower().split())
        if words & _ENGLISH_COMMON:
            return "英语"
        
        # Default Latin script → English
        return "英语"
    
    return "未知"
```

- [ ] **Step 2: 写测试验证语言检测**

新建 `test_language_detect.py`:

```python
"""Tests for language detection."""
import os, sys
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from translator import detect_language

def test_english():
    assert detect_language("hello world how are you") == "英语"

def test_german():
    assert detect_language("Hallo wie geht's dir") == "德语"

def test_french():
    assert detect_language("Bonjour ça va bien") == "法语"

def test_spanish():
    assert detect_language("Hola cómo estás") == "西班牙语"

def test_russian():
    assert detect_language("Привет как дела") == "俄语"

def test_japanese():
    assert detect_language("こんにちは元気ですか") == "日语"

def test_korean():
    assert detect_language("안녕하세요") == "韩语"

def test_chinese():
    assert detect_language("你好世界这是一个测试") == "中文"

def test_empty():
    assert detect_language("") == "未知"

def test_whitespace_only():
    assert detect_language("   ") == "未知"

if __name__ == "__main__":
    for name, fn in list(locals().items()):
        if name.startswith("test_"):
            fn()
            print(f"PASS: {name}")
    print("\n=== ALL LANGUAGE DETECTION TESTS PASSED ===")
```

- [ ] **Step 3: 运行语言检测测试**

```bash
cd "y:/翻译器项目/ets2-translator" && python test_language_detect.py
```

预期: 全部 PASS

- [ ] **Step 4: 提交**

```bash
git add translator.py test_language_detect.py
git commit -m "feat: 实现语言检测函数 detect_language"
```

### Task 11: 在翻译流程中注入语言检测

**Files:**
- Modify: `translator.py:_flush_llm`, `_flush_baidu`, `_flush_hybrid`

- [ ] **Step 1: 在 _flush_llm 中添加语言检测**

```python
# 在 _flush_llm 中，每个 DisplayMessage 创建时添加 detected_language:
# 例如 L197-L201:
                detected = detect_language(text)
                self.out_queue.put(DisplayMessage(
                    player_name=batch[0].player_name,
                    original_text=text,
                    translated_text=translated,
                    detected_language=detected,
                ))
```

同样更新 batch 分支 (L206-L213) 和错误分支 (L219-L224)。

- [ ] **Step 2: 在 _flush_baidu 中添加语言检测**

```python
# L229-L246: 每个 DisplayMessage 添加 detected_language:
                detected = detect_language(msg.text)
                self.out_queue.put(DisplayMessage(
                    player_name=msg.player_name,
                    original_text=msg.text,
                    translated_text=translated,
                    detected_language=detected,
                ))
```

- [ ] **Step 3: 在 _flush_hybrid 中添加语言检测**

```python
# L291-L310: 每个 DisplayMessage 添加 detected_language:
            detected = detect_language(msg.text)
            # ... DisplayMessage 调用中添加 detected_language=detected
```

- [ ] **Step 4: 提交**

```bash
git add translator.py
git commit -m "feat: 翻译流程中注入语言检测结果"
```

### Task 12: 新增 show_language_label 配置字段

**Files:**
- Modify: `config.py`

- [ ] **Step 1: 新增字段**

```python
# 在 AppConfig 中新增:
    show_language_label: bool = True  # show [语言] label before each message
```

- [ ] **Step 2: 提交**

```bash
git add config.py
git commit -m "feat: 新增 show_language_label 配置字段"
```

### Task 13: 渲染语言标签

**Files:**
- Modify: `overlay.py:_insert_one_at`

- [ ] **Step 1: 修改 _insert_one_at 方法**

在 `overlay.py:L536-L561`，修改 `_insert_one_at`:

```python
def _insert_one_at(self, pos, player: str, orig: str, trans: str, is_self: bool, detected_lang: str = ""):
    """Insert a single message at the given position."""
    prefix = "(You) " if is_self else ""
    tags = []
    
    # Language label
    if self.cfg.show_language_label and detected_lang and not is_self:
        tags.append(("player", f"[{detected_lang}] "))
    
    tags.append(("player", f"{prefix}["))
    # ... rest unchanged ...
```

同时更新 `_sync_display` 中对 `_insert_one_at` 的调用 (L522-L523):

```python
for i in range(self._displayed_count, new_total):
    player, orig, trans, is_self = self._messages[i]
    # _messages now may need to store detected_lang too
    detected_lang = ""
    if len(self._messages[i]) >= 5:
        detected_lang = self._messages[i][4]
    self._insert_one_at(insert_pos, player, orig, trans, is_self, detected_lang)
```

- [ ] **Step 2: 更新 add_message 存储 detected_language**

在 `add_message` 方法中 (L481):

```python
def add_message(self, player_name: str, original: str, translated: str, 
                is_self: bool = False, detected_language: str = ""):
    self._messages.append((player_name, original, translated, is_self, detected_language))
```

- [ ] **Step 3: 更新 poll_messages 传递 detected_language**

```python
# L568-L577, 更新 DisplayMessage 处理:
                if isinstance(item, DisplayMessage):
                    translated = item.translated_text
                    if item.baidu_fixed:
                        translated = f"[百度优化] {translated}"
                        baidu_count += 1
                    self.add_message(
                        item.player_name, item.original_text,
                        translated, item.is_self,
                        item.detected_language,
                    )
```

- [ ] **Step 4: 提交**

```bash
git add overlay.py
git commit -m "feat: 渲染消息语言标签"
```

### Task 14: 设置面板新增语言标签开关

**Files:**
- Modify: `main.py:_build_appearance_tab`

- [ ] **Step 1: 在外观页新增 CheckButton**

在 `_build_appearance_tab` (L716-L795) 末尾、opacity 行之前或之后添加:

```python
# 在 click_through CheckButton 之后:
self.lang_label_var = tk.BooleanVar(value=self.cfg.show_language_label)
cb2 = tk.Checkbutton(card3, text="Show Language Label / 显示语言标签",
                    variable=self.lang_label_var,
                    bg=self._CARD_BG, fg=self._TEXT,
                    font=("Microsoft YaHei", 10),
                    selectcolor=self._CARD_BG,
                    activebackground=self._CARD_BG,
                    activeforeground=self._ACCENT)
cb2.grid(row=r, column=0, columnspan=2, sticky=tk.W, padx=16, pady=(4, 12))
```

- [ ] **Step 2: 在 `_save` 中保存**

```python
show_language_label=self.lang_label_var.get(),
```

- [ ] **Step 3: 提交**

```bash
git add main.py
git commit -m "feat: 设置面板新增语言标签显示开关"
```

---

### Task 15: 最终集成测试

- [ ] **Step 1: 运行全部已有测试**

```bash
cd "y:/翻译器项目/ets2-translator" && python -m pytest test_*.py -v
```

预期: 全部 PASS

- [ ] **Step 2: 运行语言检测测试**

```bash
cd "y:/翻译器项目/ets2-translator" && python test_language_detect.py
```

预期: 全部 PASS

- [ ] **Step 3: 语法检查所有修改文件**

```bash
cd "y:/翻译器项目/ets2-translator" && python -m py_compile main.py && python -m py_compile config.py && python -m py_compile translator.py && python -m py_compile overlay.py && python -m py_compile message_types.py && echo "All files compile OK"
```

预期: `All files compile OK`

- [ ] **Step 4: 提交并打版本号**

```bash
# 更新 config.py VERSION = "v1.6.0"
git add config.py
git commit -m "chore: bump version to v1.6.0"
```

---

## 提交顺序摘要

| # | 提交信息 | 文件 |
|:--|:---|:---|
| 1 | `refactor: 语言选项统一为中文全称显示` | main.py |
| 2 | `feat: 新增 send_target_language 配置字段` | config.py |
| 3 | `feat: 发送翻译支持多种目标语言` | translator.py, overlay.py, prompts/send_prompt.txt |
| 4 | `feat: 设置面板新增发送目标语言下拉框` | main.py |
| 5 | `feat: 新增 provider_mode 配置字段` | config.py |
| 6 | `feat: 实现 Provider 顺序超时回退模式` | translator.py |
| 7 | `feat: 设置面板新增 Provider 模式选项` | main.py |
| 8 | `fix: 翻译通知提示按后端类型区分显示` | overlay.py |
| 9 | `feat: DisplayMessage 新增 detected_language 字段` | message_types.py |
| 10 | `feat: 实现语言检测函数 detect_language` | translator.py, test_language_detect.py |
| 11 | `feat: 翻译流程中注入语言检测结果` | translator.py |
| 12 | `feat: 新增 show_language_label 配置字段` | config.py |
| 13 | `feat: 渲染消息语言标签` | overlay.py |
| 14 | `feat: 设置面板新增语言标签显示开关` | main.py |
| 15 | `chore: bump version to v1.6.0` | config.py |
