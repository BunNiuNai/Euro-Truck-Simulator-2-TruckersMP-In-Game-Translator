# v1.7.0 — 广告发送器集成 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在翻译器设置面板新增「📢 广告发送」Tab，用 Python/Tkinter 复刻 Java 广告软件全部功能。

**Architecture:** 翻译器 SettingsDialog 新增第 6 个 Tab，包含基本设置卡片、消息卡片、按钮栏、日志卡片。倒计时用 `root.after()` 驱动，发送复用 `input_sender.send_chat_message()`，配置通过 `config.py` 持久化。

**Tech Stack:** Python 3.10+, Tkinter, Win32 SendInput API

---

## Task 1: 新增广告配置字段 + 修改发送延迟

**Files:**
- Modify: `config.py:L225-L229`
- Modify: `input_sender.py:L247-L255`

- [ ] **Step 1: 在 AppConfig 新增 `ad_messages` 和 `ad_countdown` 字段**

```python
# 在 config.py show_language_label 之后, llm_providers 之前添加:
    ad_messages: list[str] = field(default_factory=lambda: [""] * 5)  # 广告发送消息
    ad_countdown: str = ""  # 广告发送倒计时(分钟)
```

- [ ] **Step 2: 修改 `send_chat_message` 支持自定义延迟**

```python
# input_sender.py L247: 增加 delay_ms 参数, 默认 500ms (=Java版行为)
def send_chat_message(text: str, hotkey: str, delay_ms: int = 500) -> str | None:
    """Simulate keyboard to send a chat message in game.

    Sequence:
      1. Press hotkey to open game chat
      2. Set clipboard via Win32 API
      3. Ctrl+V to paste
      4. Enter to send

    Args:
        delay_ms: wait time between keystrokes (default 500ms for reliability)
    """
    try:
        delay_s = delay_ms / 1000.0

        # ... existing code, replace 0.3 with delay_s ...
```

更新内部等待时间:
```python
        _press_key(hk)
        time.sleep(delay_s)

        clipboard_set(text)

        _combo([VK_CONTROL, VK_V])
        time.sleep(delay_s)

        _press_key(VK_RETURN)
        time.sleep(delay_s)
```

- [ ] **Step 3: 验证编译和测试**

```bash
cd "Y:/翻译器项目/ets2-translator" && python -m py_compile config.py input_sender.py && echo "Compile OK" && python -m pytest test_*.py -q
```

预期: 全部 PASS

- [ ] **Step 4: 提交**

```bash
git add config.py input_sender.py
git commit -m "feat: 新增广告发送配置字段, send_chat_message 支持自定义延迟"
```

---

## Task 2: 构建广告发送 Tab UI

**Files:**
- Modify: `main.py` (新增 `_build_ad_tab` 方法 + tab 注册)

- [ ] **Step 1: 在 tab_names 列表中注册新 Tab**

```python
# main.py L475-L481, 追加最后一行:
        tab_names = [
            ("api", "API 配置"),
            ("hotkeys", "快捷键"),
            ("appearance", "外观"),
            ("logs", "📋 翻译器日志"),
            ("messages", "💬 消息日志"),
            ("ad", "📢 广告发送"),
        ]
```

- [ ] **Step 2: 在 _build() 中创建 ad tab frame**

在 `_build_messages_tab` 调用之后, `_switch_tab("api")` 之前:

```python
        # ---- build each tab's frame ----
        self._build_api_tab()
        self._build_hotkeys_tab()
        self._build_appearance_tab()
        self._build_logs_tab()
        self._build_messages_tab()
        self._build_ad_tab()   # <-- 新增
```

- [ ] **Step 3: 实现 `_build_ad_tab()` 方法 — 框架**

```python
    def _build_ad_tab(self):
        """广告发送 Tab — 定时循环发送游戏内广告消息."""
        frame = self._tab_frames["ad"] = tk.Frame(self._content_area, bg=self._PAGE_BG)
        frame.columnconfigure(0, weight=1)

        canvas = tk.Canvas(frame, bg=self._PAGE_BG, highlightthickness=0, bd=0)
        self._canvases["ad"] = canvas
        scrollbar = ttk.Scrollbar(frame, orient=tk.VERTICAL, command=canvas.yview)
        canvas.configure(yscrollcommand=scrollbar.set)
        canvas.grid(row=0, column=0, sticky="nsew")
        scrollbar.grid(row=0, column=1, sticky="ns")
        frame.rowconfigure(0, weight=1)

        inner = tk.Frame(canvas, bg=self._PAGE_BG, padx=20, pady=16)
        inner.columnconfigure(0, weight=1)
        inner_id = canvas.create_window((0, 0), window=inner, anchor=tk.NW)

        def _on_inner_configure(event):
            canvas.configure(scrollregion=canvas.bbox("all"))
        inner.bind("<Configure>", _on_inner_configure)

        def _on_canvas_configure(event):
            canvas.itemconfig(inner_id, width=event.width)
        canvas.bind("<Configure>", _on_canvas_configure)

        # -- 运行时状态（不持久化） --
        self._ad_running = False
        self._ad_countdown_seconds = 0
        self._ad_current_index = 0
        self._ad_timer_after_id = None
        self._ad_total_seconds = 0

        # -- 基本设置卡片 --
        self._section_label(inner, "BASIC SETTINGS  /  基本设置").pack(fill=tk.X, pady=(0, 8))
        settings_card = self._card(inner, padx=16, pady=12)
        settings_card.pack(fill=tk.X, pady=(0, 4))
        settings_card.columnconfigure(1, weight=1)

        r = 0
        # 倒计时输入
        self._label(settings_card, "Countdown (min) / 倒计时(分钟)").grid(
            row=r, column=0, sticky=tk.W, pady=5, padx=(16, 8))
        self.ad_countdown_entry = self._entry(settings_card, width=10)
        self.ad_countdown_entry.grid(row=r, column=1, sticky=tk.W, pady=5, padx=(0, 16))
        r += 1

        # 剩余时间
        self._label(settings_card, "Remaining (sec) / 剩余时间(秒)").grid(
            row=r, column=0, sticky=tk.W, pady=5, padx=(16, 8))
        self.ad_remaining_label = tk.Label(settings_card, text="0",
            bg=self._CARD_BG, fg=self._ACCENT,
            font=("Microsoft YaHei", 22, "bold"), anchor=tk.W)
        self.ad_remaining_label.grid(row=r, column=1, sticky=tk.W, pady=5, padx=(0, 16))
        r += 1

        # 当前发送
        self._label(settings_card, "Current / 当前发送").grid(
            row=r, column=0, sticky=tk.W, pady=5, padx=(16, 8))
        self.ad_current_label = tk.Label(settings_card, text="（未启动）",
            bg=self._CARD_BG, fg=self._TEXT_SEC,
            font=("Microsoft YaHei", 10, "bold"), anchor=tk.W)
        self.ad_current_label.grid(row=r, column=1, sticky=tk.W, pady=5, padx=(0, 16))
        r += 1
```

- [ ] **Step 4: 实现 `_build_ad_tab()` — 消息卡片**

```python
        # -- 广告消息卡片 --
        self._section_label(inner, "AD MESSAGES  /  广告消息").pack(fill=tk.X, pady=(8, 8))
        msg_card = self._card(inner, padx=12, pady=8)
        msg_card.pack(fill=tk.X, pady=(0, 4))
        msg_card.columnconfigure(1, weight=1)

        _MSG_COLORS = ["#e74c3c", "#e67e22", "#3cb371", "#4090e0", "#9b59b6"]
        self._ad_message_areas: list[tk.Text] = []
        self._ad_dot_labels: list[tk.Label] = []
        self._ad_msg_frames: list[tk.Frame] = []

        for i in range(5):
            # 圆点
            dot = tk.Label(msg_card, text="●", bg=self._CARD_BG, fg=_MSG_COLORS[i],
                          font=("Microsoft YaHei", 12, "bold"))
            dot.grid(row=i, column=0, sticky=tk.NW, padx=(12, 4), pady=(8, 0))
            self._ad_dot_labels.append(dot)

            # 标签
            tk.Label(msg_card, text=f"消息 {i+1}", bg=self._CARD_BG, fg=self._TEXT,
                    font=("Microsoft YaHei", 10)).grid(
                row=i, column=0, sticky=tk.NW, padx=(30, 4), pady=(10, 0))

            # 输入框容器（用于高亮）
            entry_frame = tk.Frame(msg_card, bg=self._CARD_BG)
            entry_frame.grid(row=i, column=1, sticky=tk.EW, padx=(8, 12), pady=3)
            entry_frame.columnconfigure(0, weight=1)
            self._ad_msg_frames.append(entry_frame)

            area = tk.Text(entry_frame, height=3, width=40,
                          font=("Microsoft YaHei", 10),
                          bg=self._INPUT_BG, fg=self._TEXT,
                          wrap=tk.WORD,
                          borderwidth=1, highlightthickness=1,
                          highlightbackground=self._INPUT_BORDER,
                          highlightcolor=self._ACCENT,
                          padx=8, pady=4)
            area.grid(row=0, column=0, sticky=tk.EW)
            self._ad_message_areas.append(area)
```

- [ ] **Step 5: 实现 `_build_ad_tab()` — 按钮和日志卡片**

```python
        # -- 按钮栏 --
        btn_frame = tk.Frame(inner, bg=self._PAGE_BG)
        btn_frame.pack(fill=tk.X, pady=(12, 4))

        self.ad_start_btn = tk.Label(btn_frame, text="▶  开始", bg="#3cb371", fg="#ffffff",
            font=("Microsoft YaHei", 11, "bold"), padx=28, pady=8, cursor="hand2")
        self.ad_start_btn.pack(side=tk.LEFT, padx=(0, 12))
        self.ad_start_btn.bind("<Button-1>", lambda e: self._ad_start())
        self.ad_start_btn.bind("<Enter>", lambda e: self.ad_start_btn.configure(bg="#2e965a"))
        self.ad_start_btn.bind("<Leave>", lambda e: self.ad_start_btn.configure(
            bg="#3cb371" if self.ad_start_btn.cget("state") != "disabled" else "#666666"))

        self.ad_stop_btn = tk.Label(btn_frame, text="■  暂停", bg="#e74c3c", fg="#ffffff",
            font=("Microsoft YaHei", 11, "bold"), padx=28, pady=8, cursor="hand2")
        self.ad_stop_btn.pack(side=tk.LEFT)

        # 初始: 暂停按钮不可用
        self._ad_update_button_state()

        self.ad_stop_btn.bind("<Button-1>", lambda e: self._ad_stop())
        self.ad_stop_btn.bind("<Enter>", lambda e: self.ad_stop_btn.configure(bg="#c0392b"))
        self.ad_stop_btn.bind("<Leave>", lambda e: self.ad_stop_btn.configure(
            bg="#e74c3c" if self.ad_stop_btn.cget("state") != "disabled" else "#666666"))

        # -- 发送日志卡片 --
        self._section_label(inner, "SEND LOG  /  发送日志").pack(fill=tk.X, pady=(8, 8))
        log_card = self._card(inner, padx=4, pady=4)
        log_card.pack(fill=tk.BOTH, expand=True, pady=(0, 4))

        self.ad_log_text = tk.Text(log_card, height=8, width=50,
            font=("Consolas", 9), bg=self._INPUT_BG, fg=self._TEXT_SEC,
            wrap=tk.WORD, state=tk.DISABLED,
            borderwidth=0, highlightthickness=0, padx=8, pady=6)
        self.ad_log_text.pack(fill=tk.BOTH, expand=True, padx=4, pady=4)
```

- [ ] **Step 6: 编译检查**

```bash
cd "Y:/翻译器项目/ets2-translator" && python -m py_compile main.py && echo "Compile OK"
```

- [ ] **Step 7: 提交**

```bash
git add main.py
git commit -m "feat: 构建广告发送 Tab UI 框架"
```

---

## Task 3: 实现广告发送业务逻辑

**Files:**
- Modify: `main.py` (SettingsDialog 新增方法)

- [ ] **Step 1: 实现按钮状态管理**

```python
    def _ad_update_button_state(self):
        """更新开始/暂停按钮的启用状态."""
        if self._ad_running:
            self.ad_start_btn.configure(bg="#666666", cursor="arrow")
            try:
                self.ad_start_btn.unbind("<Button-1>")
            except Exception:
                pass
            self.ad_stop_btn.configure(bg="#e74c3c", cursor="hand2",
                state=tk.NORMAL)
        else:
            self.ad_start_btn.configure(bg="#3cb371", cursor="hand2")
            self.ad_start_btn.bind("<Button-1>", lambda e: self._ad_start())
            self.ad_stop_btn.configure(bg="#666666", cursor="arrow",
                state=tk.DISABLED)
```

- [ ] **Step 2: 实现开始逻辑**

```python
    def _ad_start(self):
        """校验输入 → 启动倒计时."""
        countdown_text = self.ad_countdown_entry.get().strip()

        # 校验: 至少有一条消息
        has_msg = any(a.get("1.0", tk.END).strip() for a in self._ad_message_areas)
        if not has_msg:
            self._ad_append_log("错误: 请至少填写一条广告消息")
            return

        # 校验倒计时
        err = self._ad_validate_countdown(countdown_text)
        if err:
            self._ad_append_log(f"错误: {err}")
            return

        minutes = int(countdown_text)
        self._ad_total_seconds = minutes * 60
        self._ad_countdown_seconds = self._ad_total_seconds
        self._ad_running = True
        self._ad_current_index = -1  # tick 中跳到下一条

        # 跳到第一个非空消息
        self._ad_advance_index()

        self._ad_update_button_state()
        self._ad_highlight(self._ad_current_index)
        self.ad_remaining_label.config(text=str(self._ad_countdown_seconds))
        self._ad_append_log(f"开始发送，间隔 {minutes} 分钟")

        self._ad_schedule_tick()

    @staticmethod
    def _ad_validate_countdown(text: str) -> str | None:
        """校验倒计时。返回 None 通过，否则返回错误消息."""
        if not text:
            return "请输入倒计时时间"
        try:
            minutes = int(text)
        except ValueError:
            return "请输入有效的整数"
        if minutes <= 0:
            return "倒计时必须大于 0 分钟"
        if minutes > 1440:
            return "倒计时不能超过 1440 分钟（24 小时）"
        return None
```

- [ ] **Step 3: 实现停止逻辑**

```python
    def _ad_stop(self):
        """停止倒计时."""
        self._ad_running = False
        if self._ad_timer_after_id is not None:
            self.top.after_cancel(self._ad_timer_after_id)
            self._ad_timer_after_id = None
        self._ad_countdown_seconds = 0
        self.ad_remaining_label.config(text="0")
        self._ad_clear_highlight()
        self.ad_current_label.config(text="（未启动）")
        self.ad_current_label.config(fg=self._TEXT_SEC)
        self._ad_update_button_state()
        self._ad_append_log("已暂停")
```

- [ ] **Step 4: 实现倒计时 tick**

```python
    def _ad_schedule_tick(self):
        """每秒执行一次的倒计时回调."""
        if not self._ad_running:
            return
        self.ad_remaining_label.config(text=str(self._ad_countdown_seconds))
        if self._ad_countdown_seconds <= 0:
            # 发送当前消息
            import threading
            threading.Thread(target=self._ad_send_current, daemon=True).start()
            # 跳到下一条，重置倒计时
            self._ad_advance_index()
            self._ad_highlight(self._ad_current_index)
            self._ad_countdown_seconds = self._ad_total_seconds
        self._ad_countdown_seconds -= 1
        self._ad_timer_after_id = self.top.after(1000, self._ad_schedule_tick)

    def _ad_advance_index(self):
        """跳到下一个非空消息索引."""
        start = self._ad_current_index
        for _ in range(5):
            self._ad_current_index = (self._ad_current_index + 1) % 5
            text = self._ad_message_areas[self._ad_current_index].get("1.0", tk.END).strip()
            if text:
                return
        # 全部为空
        self._ad_current_index = (start + 1) % 5
```

- [ ] **Step 5: 实现发送**

```python
    def _ad_send_current(self):
        """在后台线程中发送当前消息."""
        text = self._ad_message_areas[self._ad_current_index].get("1.0", tk.END).strip()
        if not text:
            return

        from input_sender import send_chat_message
        idx = self._ad_current_index
        preview = text[:30] + "..." if len(text) > 30 else text

        err = send_chat_message(text, "y", delay_ms=500)
        if err:
            self.top.after(0, lambda: self._ad_append_log(f"发送失败 消息{idx+1}: {err}"))
        else:
            self.top.after(0, lambda: self._ad_append_log(f"已发送 消息{idx+1}: {preview}"))
```

- [ ] **Step 6: 实现高亮和日志**

```python
    def _ad_highlight(self, index: int):
        """高亮第 index 行消息,清除其他高亮."""
        for i in range(5):
            is_current = (i == index)
            self._ad_message_areas[i].configure(
                bg="#fff3cd" if is_current else self._INPUT_BG)
            self._ad_dot_labels[i].configure(
                text="▶" if is_current else "●",
                fg=self._ACCENT if is_current else ["#e74c3c","#e67e22","#3cb371","#4090e0","#9b59b6"][i])
        self.ad_current_label.config(
            text=f"消息 {index + 1}", fg=self._ACCENT)

    def _ad_clear_highlight(self):
        """清除所有消息高亮."""
        for i in range(5):
            self._ad_message_areas[i].configure(bg=self._INPUT_BG)
            self._ad_dot_labels[i].configure(
                text="●", fg=["#e74c3c","#e67e22","#3cb371","#4090e0","#9b59b6"][i])
        self.ad_current_label.config(text="（未启动）", fg=self._TEXT_SEC)

    def _ad_append_log(self, text: str):
        """追加日志行,超过500行裁剪最早记录."""
        from datetime import datetime
        ts = datetime.now().strftime("%H:%M:%S")
        self.ad_log_text.configure(state=tk.NORMAL)
        self.ad_log_text.insert(tk.END, f"[{ts}] {text}\n")
        # 裁剪: 保留最后500行
        line_count = int(self.ad_log_text.index("end-1c").split(".")[0])
        if line_count > 500:
            self.ad_log_text.delete("1.0", f"{line_count - 500}.0")
        self.ad_log_text.configure(state=tk.DISABLED)
        self.ad_log_text.see(tk.END)
```

- [ ] **Step 7: 编译和测试**

```bash
cd "Y:/翻译器项目/ets2-translator" && python -m py_compile main.py && echo "Compile OK" && python -m pytest test_*.py -q
```

- [ ] **Step 8: 提交**

```bash
git add main.py
git commit -m "feat: 实现广告发送业务逻辑（开始/暂停/倒计时/发送/高亮/日志）"
```

---

## Task 4: 配置持久化集成

**Files:**
- Modify: `main.py` (`_load_values`, `_save`)

- [ ] **Step 1: `_load_values` 恢复广告消息和倒计时**

```python
# 在 _load_values 末尾添加:
        # 恢复广告发送数据
        for i in range(5):
            if i < len(self.cfg.ad_messages):
                msg = self.cfg.ad_messages[i]
                if msg:
                    self._ad_message_areas[i].insert("1.0", msg)
        self.ad_countdown_entry.insert(0, self.cfg.ad_countdown)
```

- [ ] **Step 2: `_save` 保存广告消息和倒计时**

```python
# 在 _save 的 AppConfig(...) 构造中, 在 llm_providers 之前添加:
            ad_messages=[a.get("1.0", tk.END).strip() for a in self._ad_message_areas],
            ad_countdown=self.ad_countdown_entry.get().strip(),
```

- [ ] **Step 3: 编译和测试**

```bash
cd "Y:/翻译器项目/ets2-translator" && python -m py_compile main.py && echo "Compile OK" && python -m pytest test_*.py -q
```

- [ ] **Step 4: 提交**

```bash
git add main.py
git commit -m "feat: 广告发送数据持久化 (config.json)"
```

---

## Task 5: 验证测试 + 版本号

- [ ] **Step 1: 运行全部测试**

```bash
cd "Y:/翻译器项目/ets2-translator" && python -m pytest test_*.py -v
```

预期: 全部 PASS (45 tests)

- [ ] **Step 2: 语法检查所有修改文件**

```bash
cd "Y:/翻译器项目/ets2-translator" && python -m py_compile main.py config.py input_sender.py && echo "All files compile OK"
```

- [ ] **Step 3: Bump 版本号**

```python
# config.py:
VERSION = "v1.7.0"
```

- [ ] **Step 4: 提交**

```bash
git add config.py
git commit -m "chore: bump version to v1.7.0"
```

---

## 提交顺序摘要

| # | 提交信息 | 文件 |
|:--|:---|:---|
| 1 | `feat: 新增广告发送配置字段, send_chat_message 支持自定义延迟` | config.py, input_sender.py |
| 2 | `feat: 构建广告发送 Tab UI 框架` | main.py |
| 3 | `feat: 实现广告发送业务逻辑` | main.py |
| 4 | `feat: 广告发送数据持久化 (config.json)` | main.py |
| 5 | `chore: bump version to v1.7.0` | config.py |
