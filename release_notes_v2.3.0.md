# v2.3.0 — 术语处理重构 + 翻译质量优化

## 🧠 五层术语处理（全新）

参考业界优秀的 ETS2 翻译插件方案，用「五层术语处理」替换旧的提示词术语库：

1. **本地俚语词典**（约 110 条）— `wtf→什么鬼`、`sry→抱歉`、`rec→已录屏`、`ram→撞人` 等中英土俄多语俚语，**直接本地命中，零 API 调用**
2. **本地短语回退** — 俄语/德语/土耳其语短语、系统消息、颜文字精确匹配
3. **结构化短语** — `rec ban→已录屏等封禁`、`report [名字]→举报[名字]`
4. **Prompt 内嵌映射** — 让 LLM 也遵守 `sry=抱歉, ty/thx=谢谢, rec=已录屏, wtf=什么鬼`
5. **后处理补译** — LLM 漏译的英文缩写自动补译

## ✨ ETS2 专有词汇接入

`truck→卡车`、`trailer→挂车`、`convoy→车队`、`gas station→加油站` 等 ETS2 专有名词，单词/短语直接本地命中。

## 🚀 发消息方式升级

- 采用 **system + user** 提示词结构（原来是单条 user 消息）
- `temperature=0`（原来是 0.2，输出更稳定）
- **动态 max_tokens**（按输入长度计算，不再固定 500）
- DeepSeek / MiMo 自动关闭 thinking（避免思考链消耗 token）

## 🛡️ 翻译质量加固

- **@玩家名保留** — `@xxx hello` 翻译后 @名字 不被吞掉
- **未翻译校验** — LLM 返回原文/非目标语言时自动触发轮转回退下一家
- **Prompt 加固** — 强化「只输出译文、禁止解释/描述/回显原文」，抑制小模型"解释文字"现象

## 🐛 修复

- **批量翻译分隔符泄漏** — LLM 未按分隔符回显时，逐条回退单独翻译，避免整段译文挂到第一条消息
- 移除旧的失效术语提示词文件（receive_prompt.txt / send_prompt.txt）

## 📦 其他

- 版本号更新至 v2.3.0
- 清理残留 worktree 分支
